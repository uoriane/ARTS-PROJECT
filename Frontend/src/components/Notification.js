import React from 'react';

const Notifications = () => (
  <div style={{ background: "#fffae6", padding: "0.5em" }}>
    <strong>Notifications:</strong> You have 2 new audit recommendations to review.
  </div>
);
export default Notifications;