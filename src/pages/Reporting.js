import React from 'react';

const Reporting = () => (
  <div>
    <h2>Audit Recommendation Reporting</h2>
    <p>Filter by department, status, or export as Excel/PDF (future).</p>
    <ul>
      <li>Reporting graphs and filter controls here.</li>
      <li>Export to Excel/PDF buttons (stubbed).</li>
    </ul>
  </div>
);
export default Reporting;